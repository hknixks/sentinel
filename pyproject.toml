[project]
name = "sentinel"
version = "0.1.0"
description = "Real-time market intelligence and trade signal system (Phase 1: market-data engine)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.setuptools.packages.find]
where = ["src"]
